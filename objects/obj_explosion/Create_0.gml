explosion_radius = 0; // Initialize the starting radius of the explosion
max_radius = 500;     // Maximum explosion radius
expansion_speed = 5;  // Speed of radius growth
alarm[0] = 60;        // Explosion lasts 0.5 seconds

show_debug_message("Create Event: explosion_radius initialized to " + string(explosion_radius));