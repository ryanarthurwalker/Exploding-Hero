sprite_index = spr_freeze_bomb; // Ensure the correct sprite is set
depth = -10; // Ensure it draws on top of other objects

starting_x = x;
starting_y = y;
max_distance = 200; // Set the maximum distance the bomb can travel