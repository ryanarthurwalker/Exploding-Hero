angle_to_player = point_direction(x, y, obj_player.x, obj_player.y);
speed = 0;  // Initialize speed
invulnerable = false; // Shield is not active by default
shield_active = false

image_xscale = 0.2;
image_yscale = 0.2; 